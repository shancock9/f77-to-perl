      a=sqrt(3.)
      print *,'square root of 3 =',a
      pi = 4*atan2(1.,1.)
      print *,'4*atan2(1.,1.)=pi=',pi
      pi=4*atan(1.0)
      print *,'4*atan(1.0)=pi=',pi
      one=tan(pi/4)
      print *, 'tan(pi/4) = one=',one
      epi = exp(pi)
      print *,'exp(pi)=',epi
      rlepi = alog(pi)
      print *,'log(pi)=',rlepi
      spi = sin(pi)
      print *,'sin(pi)=',spi
      cpi = cos(pi)
      print *,'cos(pi)=',cpi

      ampi = abs(-pi)
      print *,'abs(-pi)=',ampi
      ipi = int(pi)
      print *,'int(pi)=',ipi

      j=ifix(2.3)
      print *, 'ifix(2.3)=',j

      ff=max(2.3,3.3,4.4,5.5)
      print *, 'max(2.3,3.3,4.4,5.5)=',ff
      end
